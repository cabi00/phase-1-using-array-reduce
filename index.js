const batteryBatches = [4, 5, 3, 4, 4, 6, 5];

const totalAmountOfBatteries = (total, battery) => {
    console.log (total, battery);
    let totalBatteries =  battery;
        return total += totalBatteries;
}
    let totalBatteries = batteryBatches.reduce(totalAmountOfBatteries,0)

    
  

  